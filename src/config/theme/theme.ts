import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1B3764",
      contrastText: "#F6F8FC",
    },
    secondary: {
      main: "#FFCA42",
      contrastText: "#1B3764",
    },
  },
});

export default theme;
